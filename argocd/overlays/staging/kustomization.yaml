apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dex-staging

resources:
  - ../../base

patches:
  - target:
      kind: Deployment
      name: dex-app
    patch: |
      - op: replace
        path: /spec/replicas
        value: 2

commonLabels:
  environment: staging
