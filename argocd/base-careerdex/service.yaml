apiVersion: v1
kind: Service
metadata:
  name: careerdex
  labels:
    app: careerdex
spec:
  type: ClusterIP
  ports:
    - port: 8788
      targetPort: 8788
      protocol: TCP
      name: app
  selector:
    app: careerdex
