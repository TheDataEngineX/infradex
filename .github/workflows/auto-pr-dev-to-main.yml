name: Auto PR — dev → main

on:
  push:
    branches:
      - dev

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-pr:
    uses: TheDataEngineX/.github/.github/workflows/auto-pr-dev-to-main.yml@main
    secrets: inherit
